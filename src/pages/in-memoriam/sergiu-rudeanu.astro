---
import Carousel from "@/components/Carousel";
import Layout from "@/layouts/Layout.astro";

const images = (await Astro.glob("../../assets/sergiu_rudeanu/*.jpg")).map(
    (image) => image.default as string
);
---

<Layout title="Istrail Lab | Sergiu Rudeanu">
    <h2 class="text-2xl font-bold py-4">Sergiu Rudeanu</h2>
    <Carousel images={images} client:only="react" />
</Layout>
