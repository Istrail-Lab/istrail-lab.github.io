---
import Carousel from "@/components/Carousel";
import Layout from "@/layouts/Layout.astro";

const images = (await Astro.glob("../../assets/dragos_vaida/*.jpg")).map(
    (image) => image.default as string
);
---

<Layout title="Istrail Lab | Dragos Vaida">
    <h2 class="text-2xl font-bold py-4">Dragos Vaida</h2>
    <Carousel images={images} client:only="react" />
</Layout>
