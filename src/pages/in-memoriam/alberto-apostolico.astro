---
import Carousel from "@/components/Carousel";
import Layout from "@/layouts/Layout.astro";

const images = (await Astro.glob("../../assets/alberto_apostolico/*.jpg")).map(
    (image) => image.default as string
);
---

<Layout title="Istrail Lab | Alberto Apostolico">
    <h2 class="text-2xl font-bold py-4">Alberto Apostolico</h2>
    <Carousel images={images} client:only="react" />
</Layout>
